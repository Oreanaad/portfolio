import styled from "styled-components";

export const ProjectsDiv = styled.div`
    /* Padding rather than margin, so it cannot collapse out to the page
       container and let the cards slide under the fixed navbar. */
    padding-top: 100px;
    padding-bottom: 25px;
`